{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            "command": " g++ -v -g src/main.cpp -o build/main.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}